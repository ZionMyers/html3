Video 1:
# What does CSS stand for?
Cascading Style Sheets
# How is CSS used within a webpage?
to change the words and imgs
# Can CSS work without HTML?
no cause it needs html
# What does CSS consist of?
CSS describes how elements should be rendered on screen, on paper, in speech, or on other media.
# Is the goal to learn all of the CSS properties? If not, what is the goal for learning CSS?
CSS describles the visual style and present
# What is the syntax for CSS?
look at syntaxexample.
# What is a selector?
somethin that selects one section like h1
# What are Declaration(s)?
value
# What is a Declaration Property?
look at syntax example
# What is a Declaration Value?
property.
# What is a Declaration Block?
look at syntaxexample2
	Video 2:
# What are the 3 ways to connect CSS to HTML?
to do style"color: blue>
style. /style.
and u can make a style.css
# What is inline CSS?
Inline CSS is the technique to define the single element with the insert style sheets in an HTML document.
# What is internal CSS?
Internal CSS is a form of CSS using which you can add CSS to HTML documents.
# What is external CSS?
External CSS is a form of CSS which is used to add styling to multiple HTML pages at a time
# Which CSS approach uses an attribute to add style
CSS can be added to HTML documents in 3 ways: Inline - by using the style attribute inside HTML elements
# Which of the 3 ways to add CSS is the best approach and why? Which is the worst and why?
external is the best one
# Should CSS code be contained within an HTML file? Why or Why not?
Do we use the link element to hyperlink to another page?
no cause u can use ./file.example
Which CSS approach utilizes the link element?
link
Video 3: (Use W3schools to support answers)
# what does the font-size property do? 
The font-size CSS property sets the size of the font
# what does the font-family property do?
he font-family property specifies the font for an element.
# what does the text-transform property do?
The text-transform CSS property specifies how to capitalize an element's text
# what does the font-style property do?
he font-style CSS property sets whether a font should be styled with a normal, italic, or oblique face from its font-family 
# what does the line-height property do?
The line-height CSS property sets the height of a line box. It's commonly used to set the distance between lines of text
# what are CSS units and how are they different? (ex. px, em, %, etc.)
CSS provides us with lots of units, some of whose values are fixed and are called absolute units while there are others whose values are relative to other values like that of the parent element's or to the default value for that particular HTML element, these are called relative units.
Video 4: (Use W3schools to support answers)
# How are Classes & ID’s utilized in CSS?
When working in CSS, IDs and classes can identify and block elements together to ensure they share design elements.
# what is the difference between Classes & ID’s?
While an ID is specific to a single element, classes can be assigned to multiple elements on a page or throughout the website
# what symbol is used for ID’s in CSS?
the hashtag
# what symbol is used for classes in CSS?
To select elements with a specific class, write a period (.) character, followed by the class name
